# Optional. Without a key, Scripter runs a polished on-device demo assistant.
# Your scripts are not used to train models.
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4o-mini
# Optional OpenAI-compatible base URL (default: https://api.openai.com/v1)
VITE_OPENAI_BASE_URL=
